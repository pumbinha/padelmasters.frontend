---
export interface UserLinkProps {
	userId: string;
	displayName: string;
	class?: string;
}

const props = Astro.props as UserLinkProps;
const { userId, displayName } = props;

const classes = props.class || "text-primary-600 hover:text-primary-800 transition-colors underline decoration-primary-300 hover:decoration-primary-500";
---

<a href={`/players/${userId}`} class={classes}>
	<span class="decoration-primary-500">
		{displayName}
	</span>
</a>
